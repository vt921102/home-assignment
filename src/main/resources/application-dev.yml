spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  flyway:
    locations:
      - classpath:db/migration
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}