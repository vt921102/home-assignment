spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/flashsale
    username: app
    password: app_password
  data:
    redis:
      host: redis
  flyway:
    locations:
      - classpath:db/migration